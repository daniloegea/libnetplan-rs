[package]
name = "netplan-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[workspace]
members = ["commands", "libnetplan"]

[dependencies]
clap = "4.4"
commands = { path = "./commands" }
